return{
    https=https,
    lg=lg,
    love=love,
    math=math,
    string=string,
    table=table,
    pairs=pairs,
    ipairs=ipairs,
    input=input,
    color=color,
    clamp=clamp,
    lerp=lerp,
    lerpDt=lerpDt,
    col=col,

    init=function() end,

    update=function() end,

    draw=function() end,

    gW=config.gameWidth,
    gH=config.gameHeight,
    wW=config.windowWidth,
    wH=config.windowHeight
}